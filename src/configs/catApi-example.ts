// Documenentation: https://docs.thecatapi.com/authentication
// After you are done configuring, rename to "catApi.ts"
const catApi = {
    endpoint: "your-endpoint",
    key: "your-api-key"
}

export default catApi;